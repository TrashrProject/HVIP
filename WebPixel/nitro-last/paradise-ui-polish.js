(() => {
  'use strict';

  const VERSION = '83.0.0-event-driven-polish';
  const HUD_ID = 'paradise-rp-hud';

  const esc = value => String(value ?? '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
  const toNumber = value => Number.isFinite(Number(value)) ? Number(value) : null;
  const format = value => { const n=toNumber(value); return n===null?'':new Intl.NumberFormat('fr-FR').format(n); };
  const paths={phone:'<rect x="7" y="2.8" width="10" height="18.4" rx="2"/><path d="M10.4 5h3.2M11 18.2h2"/>',heart:'<path d="M20.8 4.6a5.4 5.4 0 0 0-7.6 0L12 5.8l-1.2-1.2a5.4 5.4 0 1 0-7.6 7.6L12 21l8.8-8.8a5.4 5.4 0 0 0 0-7.6Z"/>',shield:'<path d="M12 3 20 6v6c0 4.6-3 7.2-8 9-5-1.8-8-4.4-8-9V6l8-3Z"/>'};
  const icon=name=>`<span class="pr4-icon"><svg viewBox="0 0 24 24" aria-hidden="true">${paths[name]||paths.phone}</svg></span>`;

  function avatarUrl(data){ const raw=String(data?.avatar_url||'').trim(); if(raw)return raw; const look=String(data?.look||'').trim(); if(look&&/^[a-z0-9.\-]+$/i.test(look))return `../avatar-image.php?figure=${encodeURIComponent(look)}&direction=2&head_direction=3&gesture=sml&action=std&size=l&hud=83`; return ''; }
  function ratio(current,max){ const c=toNumber(current),m=toNumber(max); if(c===null||m===null||m<=0)return null; return Math.max(0,Math.min(100,Math.round(c/m*100))); }
  function statLine(kind,label,current,max){ const percent=ratio(current,max),c=toNumber(current),m=toNumber(max); if(percent===null||c===null||m===null)return''; let tone=kind; if(kind==='health')tone=percent<=25?'health danger':percent<=55?'health warn':'health'; return `<div class="pr4-polish-stat ${tone}"><span>${icon(kind==='armor'?'shield':'heart')}<b>${esc(label)}</b></span><em>${Math.round(c)} / ${Math.round(m)}</em><i><u style="width:${percent}%"></u></i></div>`; }

  function patchBottomNavigation(root){ const left=root.querySelector('.pr4-dock-left'),right=root.querySelector('.pr4-dock-right'); if(!left||!right)return; left.querySelectorAll('[data-pr4-action="open:phone"]').forEach(btn=>btn.remove()); right.querySelectorAll('[data-pr4-action="open:social"], [data-pr4-action="open:notifications"]').forEach(btn=>btn.remove()); let phoneBtn=right.querySelector('[data-pr4-polish-phone="1"]'); const moreBtn=right.querySelector('[data-pr4-action="toggle-more"]'); if(!phoneBtn){ phoneBtn=document.createElement('button'); phoneBtn.type='button'; phoneBtn.className='app pr4-polish-phone'; phoneBtn.dataset.pr4PolishPhone='1'; phoneBtn.dataset.tip='Téléphone · P'; phoneBtn.innerHTML=`${icon('phone')}<span>Téléphone</span>`; phoneBtn.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();window.__ParadiseRPUI?.open?.('phone');}); right.insertBefore(phoneBtn,moreBtn||right.firstChild); } if(moreBtn){ moreBtn.classList.add('pr4-polish-actions'); moreBtn.dataset.tip='Actions rapides'; const label=moreBtn.querySelector('span'); if(label)label.textContent='Actions'; } }

  function patchProfile(root,data){ if(!data||data.ok===false)return; const card=root.querySelector('.pr4-player-card'); if(!card)return; const username=String(data.username||'Joueur'),role=String(data.role||'Citoyen'),job=String(data.job||data.métier||data.profession||''),location=String(data.room||data.district||data.city||''),ava=avatarUrl(data),armor=data.armor||data.shield||data.armour||null,health=data.health||null; const stats=[statLine('health','Santé',health?.current,health?.max),statLine('armor','Armure',armor?.current,armor?.max)].filter(Boolean).join(''); card.classList.add('pr4-player-card-polished'); card.innerHTML=`<span class="pr4-player-avatar">${ava?`<img src="${esc(ava)}" alt="${esc(username)}">`:'<b>RP</b>'}<i></i></span><span class="pr4-player-text"><strong>${esc(username)}</strong><span class="pr4-player-tags"><em>${esc(role)}</em>${job?`<em class="job">${esc(job)}</em>`:''}</span>${location?`<small>${esc(location)}</small>`:''}${stats?`<span class="pr4-player-stats">${stats}</span>`:''}</span>`; }
  function patchStatus(root,data){ if(!data||data.ok===false)return; const money=data.money||{},status=root.querySelector('.pr4-status'); if(!status)return; const cash=status.querySelector('.cash b'),bank=status.querySelector('.bank b'),staff=status.querySelector('.staff b'),time=status.querySelector('.time b'),notif=status.querySelector('.notif b'); if(cash&&toNumber(money.cash??money.credits)!==null)cash.textContent=`${format(money.cash??money.credits)} $`; if(bank&&toNumber(money.bank)!==null)bank.textContent=`${format(money.bank)} $`; if(staff)staff.textContent=String(data.role||data.job||'Citoyen'); if(time)time.textContent=String(data.time||new Date().toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})); if(notif)notif.textContent=format(data.notifications?.count??data.notifications_count??0)||'0'; }
  function apply(data=window.__ParadiseRPUI?.getData?.()){ const root=document.getElementById(HUD_ID); if(!root)return; patchBottomNavigation(root); patchProfile(root,data); patchStatus(root,data); }
  function onData(event){ apply(event.detail||null); }
  function boot(){ apply(); window.addEventListener('paradise:player-data',onData,false); window.__ParadiseRPPolish={version:VERSION,refresh:apply}; }

  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
