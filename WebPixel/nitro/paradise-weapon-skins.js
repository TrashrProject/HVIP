(()=>{'use strict';
const labels={tazor:'Arme de choc',ak47:'AK47',akm:'AKM',g36:'G36'};let root=null,data=[],filterKey=null,busy=false;const api='/weapon-skins.php';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const weaponKeyFromName=name=>{const n=String(name||'').toLowerCase().replace(/[^a-z0-9]/g,'');if(n.includes('ak47'))return'ak47';if(n==='akm'||n.includes('fusilakm'))return'akm';if(n.includes('g36'))return'g36';if(n.includes('tazor')||n.includes('tazer'))return'tazor';return null};
function sendCommand(command){const input=document.querySelector('.nitro-chat-input-container .chat-input, input.chat-input');if(!input)return false;const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value')?.set;setter?setter.call(input,command):(input.value=command);input.dispatchEvent(new Event('input',{bubbles:true}));input.focus();setTimeout(()=>input.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',code:'Enter',keyCode:13,which:13,bubbles:true})),40);return true}
async function reapplyIfEquipped(weaponKey){try{const r=await fetch('/inventory.php?t='+Date.now(),{credentials:'same-origin',cache:'no-store'});const j=await r.json();if(!r.ok||!j.ok)return false;const equipped=(j.slots||[]).find(s=>Number(s.slot_index)===0);if(!equipped||weaponKeyFromName(equipped.display_name)!==weaponKey)return false;return sendCommand(':equiper '+equipped.display_name)}catch(_){return false}}
async function load(){const r=await fetch(api+'?t='+Date.now(),{credentials:'same-origin',headers:{Accept:'application/json'},cache:'no-store'});const j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||'Chargement impossible');data=j.skins;render()}
function render(){if(!root)return;const visible=filterKey?data.filter(s=>s.weapon_key===filterKey):data;const by={};visible.forEach(s=>(by[s.weapon_key]??=[]).push(s));const body=root.querySelector('.pws-body');body.innerHTML=Object.keys(by).map(k=>{const skins=by[k],active=skins.find(s=>s.equipped)||skins.find(s=>s.is_default)||skins[0];return `<section class="pws-group" data-weapon="${esc(k)}"><div class="pws-group-title">${esc(labels[k]||k)}</div><div class="pws-preview"><img class="pws-weapon" src="weapon-skins/${esc(active.image)}" alt=""><span>${esc(active.name)}</span></div><div class="pws-options">${skins.map(s=>`<button class="pws-option ${s.equipped?'equipped':s.owned?'owned':'locked'}" data-id="${s.id}" ${s.owned&&!busy?'':'disabled'}><span class="pws-check">${s.equipped?'✓':'•'}</span><span>${esc(s.name)}</span></button>`).join('')}</div><div class="pws-avatar-wrap"><img class="pws-avatar" src="weapon-skins/${esc(active.avatar_image)}" alt=""><small>Aperçu</small></div></section>`}).join('')||'<div class="pws-empty">Aucun skin disponible pour cette arme.</div>';body.querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>equip(+b.dataset.id)))}
function applySelectedSkin(payload){if(!payload||!payload.weapon_key)return;const detail={weaponKey:String(payload.weapon_key),effectId:Number(payload.effect_id||0),skinId:Number(payload.skin_id||0)};window.ParadiseSelectedWeaponSkin=detail;window.dispatchEvent(new CustomEvent('paradise:weapon-skin-changed',{detail}));try{localStorage.setItem('paradise.weaponSkin.'+detail.weaponKey,String(detail.effectId))}catch(_){}}
async function equip(id){if(busy)return;const status=root?.querySelector('.pws-status');const selected=data.find(s=>s.id===id);if(!selected)return;busy=true;if(status){status.classList.remove('is-error');status.textContent='Application du skin…'}render();try{const r=await fetch(api,{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json','X-Requested-With':'XMLHttpRequest'},body:JSON.stringify({skin_id:id})});const j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||'Impossible d’équiper ce skin.');data.forEach(s=>{if(s.weapon_key===selected.weapon_key)s.equipped=s.id===id});applySelectedSkin(j);render();const live=await reapplyIfEquipped(selected.weapon_key);if(status)status.textContent=live?'Skin appliqué immédiatement.':'Skin enregistré. Il sera utilisé au prochain équipement.';setTimeout(()=>window.ParadiseInventory?.refresh?.(),500)}catch(e){if(status){status.classList.add('is-error');status.textContent=e.message}await load().catch(()=>{})}finally{busy=false;render()}}
function close(){root?.remove();root=null;filterKey=null}
function open(weaponKey=null){filterKey=weaponKey||null;if(root){render();return}root=document.createElement('div');root.className='pws-backdrop';root.innerHTML=`<div class="pws-window" role="dialog" aria-modal="true" aria-label="Skins d'armes"><div class="pws-title"><span>${filterKey?'Skins • '+esc(labels[filterKey]||filterKey):'Skins d’armes'}</span><button class="pws-close" aria-label="Fermer">×</button></div><div class="pws-toolbar"><span class="pws-tab active">Skins</span>${filterKey?'<button class="pws-show-all" type="button">Toutes les armes</button>':''}</div><div class="pws-body"><div class="pws-empty">Chargement…</div></div><div class="pws-status">Choisis un skin pour l’équiper.</div></div>`;document.body.append(root);root.querySelector('.pws-close').onclick=close;root.querySelector('.pws-show-all')?.addEventListener('click',()=>{filterKey=null;root.querySelector('.pws-title span').textContent='Skins d’armes';root.querySelector('.pws-show-all')?.remove();render()});root.addEventListener('mousedown',e=>{if(e.target===root)close()});load().catch(e=>{root.querySelector('.pws-body').innerHTML=`<div class="pws-empty">${esc(e.message)}</div>`})}
document.addEventListener('click',e=>{const b=e.target.closest('button[title="Paramètres"],button[aria-label="Paramètres"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();open()},true);window.ParadiseWeaponSkins={open,close};})();
