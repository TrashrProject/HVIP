(()=>{'use strict';const labels={tazor:'Arme de choc',ak47:'Fusil AK-47',akm:'Fusil AKM',g36:'Fusil G36'};let root=null,data=[];const api='/weapon-skins.php';
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){const r=await fetch(api,{credentials:'same-origin',headers:{Accept:'application/json'}});const j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||'Chargement impossible');data=j.skins;render();}
function render(){if(!root)return;const by={};data.forEach(s=>(by[s.weapon_key]??=[]).push(s));const body=root.querySelector('.pws-body');body.innerHTML=Object.keys(by).map(k=>{const skins=by[k],active=skins.find(s=>s.equipped)||skins[0];return `<section class="pws-group"><h3>${esc(labels[k]||k)}</h3><img class="pws-weapon" src="weapon-skins/${esc(active.image)}" alt=""><div class="pws-options">${skins.map(s=>`<button class="pws-option ${s.equipped?'equipped':s.owned?'owned':'locked'}" data-id="${s.id}" ${s.owned?'':'disabled'} title="${s.owned?'Sélectionner':'Skin verrouillé'}">${esc(s.name)}</button>`).join('')}</div><img class="pws-avatar" src="weapon-skins/${esc(active.avatar_image)}" alt=""></section>`}).join('')||'<div class="pws-empty">Aucun skin configuré.</div>';body.querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>equip(+b.dataset.id)));}
async function equip(id){const status=root.querySelector('.pws-status');status.textContent='Sauvegarde…';try{const r=await fetch(api,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-Requested-With':'XMLHttpRequest'},body:JSON.stringify({skin_id:id})});const j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||'Erreur');const selected=data.find(s=>s.id===id);data.forEach(s=>{if(s.weapon_key===selected.weapon_key)s.equipped=s.id===id});status.textContent='Skin équipé.';render();}catch(e){status.textContent=e.message;}}
function close(){root?.remove();root=null}function open(){if(root)return;root=document.createElement('div');root.className='pws-backdrop';root.innerHTML='<div class="pws-window" role="dialog" aria-modal="true" aria-label="Skins d\'armes"><div class="pws-title">Configurations<button class="pws-close" aria-label="Fermer">×</button></div><div class="pws-tabs"><span class="pws-tab active">Skins</span></div><div class="pws-body"><div class="pws-empty">Chargement…</div></div><div class="pws-status"></div></div>';document.body.append(root);root.querySelector('.pws-close').onclick=close;root.addEventListener('mousedown',e=>{if(e.target===root)close()});load().catch(e=>root.querySelector('.pws-body').innerHTML=`<div class="pws-empty">${esc(e.message)}</div>`)}
document.addEventListener('click',e=>{const b=e.target.closest('button[title="Paramètres"],button[aria-label="Paramètres"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();open()},true);window.ParadiseWeaponSkins={open,close};})();
